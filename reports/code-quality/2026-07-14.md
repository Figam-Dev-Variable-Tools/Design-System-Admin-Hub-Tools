# Code Quality 리포트 — 2026-07-14

> 생성: `@tds/code-quality` (A83 Clean Code Inspector) — 기계 생성 전용, 수기 편집 금지
> 판정은 **6축의 수치 위반**으로만 한다. "읽기 좋다/나쁘다"는 판정 사유가 아니다.

- 판정: **WARN** (exit 0) — blocker 0건 · major 1건
- 정책: blocker ≥ 1 → exit 1 (PR 차단) / major 만 → exit 0 (경고, 리뷰 evidence)
- 스캔: apps/admin/src, packages/ui/src — 파일 188건 · 27086줄

| # | 축 | 심각도 | 측정값 | 임계값 | 측정 범위 | 판정 |
|---|---|---|---|---|---|---|
| 1 | 페이지 간 결합 (pages/A → pages/B) | **blocker** | 0건 | 0건 | pages 파일 84건 | PASS |
| 2 | 도메인 누수 (공통 모듈 → 도메인 타입/상수) | **blocker** | 0건 | 0건 | 공통 모듈 파일 82건 (apps/admin/src/shared/ui/, packages/ui/src/) | PASS |
| 3 | 중복 코드 (정규화 블록 해시) | major | 1건 | 30줄 이상 블록이 2회 이상 반복 = 0건 | 소스 188건 · 정규화 16460줄 (윈도 30줄) | VIOLATED |
| 4 | 순환 복잡도 (함수 단위 cyclomatic complexity) | major | 0건 | 함수당 <= 15 | 함수 1572개 (최대 복잡도 15) | PASS |
| 5 | 죽은 코드 (미사용 export) | major | 0건 | 0건 | 정의부 export 497개 | PASS |
| 6 | 레이어 역방향 의존 (atoms → molecules/organisms/…) | **blocker** | 0건 | 0건 | packages/ui 레이어 파일 60건 | PASS |

## 축 3 — 중복 코드 (정규화 블록 해시) (1건, major)

임계값: **30줄 이상 블록이 2회 이상 반복 = 0건**

| 위치 (file:line) | 심볼 | 측정값 | 조치 |
|---|---|---|---|
| `apps/admin/src/pages/product-registration/styles.ts:329`<br/>↔ `apps/admin/src/shared/ui/styles.ts:178` | `clone:7e74814c5e72b014` | 정규화 30줄 블록이 2곳에서 반복 (임계 30줄 · 2회) | 공통 부분을 함수/컴포넌트로 추출한다. 두 곳 이상이 쓰면 공통 모듈(shared/ui · packages/ui)로 올린다. |

## 조치 주체

- A83은 **측정만** 한다 — 위반 코드를 수정하지 않는다. 수정은 소유자의 일이다 (P1 단일 소유권).
  - `apps/**` → A40/A41 · `packages/**` → A30/A31/A32
- 임계값에 이의가 있으면 A01에 **ADR**을 요청한다. 임계값은 도구가 임의로 바꾸지 않는다.
