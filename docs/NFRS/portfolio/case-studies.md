# 성공 사례 — 비기능 명세서

| 항목 | 내용 |
|---|---|
| 자원명 | 성공 사례 |
| 화면 ID | `SCR-PORTFOLIO-CASE-STUDIES` · `SCR-PORTFOLIO-CASE-STUDIES-FORM` |
| 메뉴 경로 | 비즈니스 > 포트폴리오 관리 > 성공 사례 |
| 작성자 | Admin Hub 문서화 |
| 작성일 | 2026-07-22 |
| 버전 | 1.0 |
| 관련 기능 명세서 | [목록](../../FSD/portfolio/case-studies/index.md) · [등록·수정](../../FSD/portfolio/case-studies/form.md) |

**이 자원의 성격** — [포트폴리오](items.md) 와 **같은 껍데기·같은 공유 부품**을 쓴다. 여덟 절의 값이 거의 같으므로 그 문서가 한 벌을 갖고, 이 문서는 **차이만** 적는다. 차이의 뿌리는 하나다: **거르는 축이 운영자가 만드는 분류가 아니라 코드 안의 고정 업종이라는 것.**

## 1. 성능

| 항목 | 요구 | 이 자원의 값 | 측정 방법 |
|---|---|---|---|
| 전 항목 | [포트폴리오](items.md) 준수 | 같다 — **페이지네이션도 가상화도 없다** | 코드 확인 |
| 조회 건수 | — | **선택지 조회가 없다** — 목록·폼 모두 요청이 포트폴리오보다 한 건 적다. 업종이 코드 안의 상수다 — `apps/admin/src/pages/portfolio/case-studies/types.ts:51-58` | 코드 확인 |

## 2. 사용성

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 전 항목 | [포트폴리오](items.md) 준수 | 같다 — 노출 토글 열과 미디어 하단부를 **같은 구현으로** 공유한다 |
| 목록에서 읽히는 것 | 훑는 목적에 맞는 열을 보여 준다 | 넷째 열이 일자가 아니라 **성과**다 — 사례를 훑는 목적은 '언제 한 일인가'가 아니라 '무엇을 이뤘는가'다. 일자는 정렬 축으로만 남는다 — `apps/admin/src/pages/portfolio/case-studies/CaseStudyListPage.tsx:110` |
| 빈 선택지를 두지 않기 | 존재할 이유가 없는 상태를 만들지 않는다 | 폼의 업종 셀렉트에 `분류 선택` 같은 **빈 항목이 없다** — 고정 6종이라 고르지 않은 상태가 성립하지 않는다 — `apps/admin/src/pages/portfolio/case-studies/CaseStudyFormPage.tsx:134-138` |
| 세 문단을 요구하는 이유 | — | 과제·해결·성과 중 하나만 비어도 사례가 이야기로 성립하지 않아 셋 다 필수다 |

## 3. 보안

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 권한 | [포트폴리오](items.md) 준수 | **리소스 키만 다르다** — 잎 `/portfolio/case-studies`. 등록 버튼 숨김 · 노출 토글 잠금이 같은 방식으로 걸린다 |
| 플랜(엔타이틀먼트) | 공통 준수 | **걸리지 않는다** |
| 개인정보 마스킹 | — | **해당 없음** — 사람 관련 값은 `고객사`(법인명)뿐이다 |
| 입력값 검증 | [포트폴리오](items.md) 준수 | 같다. **업종만 검증 방식이 다르다** — '골랐는가'가 아니라 **'허용 6종 안의 값인가'** 를 본다. 셀렉트에 빈 항목이 없어 비어 있을 수 없고, 막아야 하는 것은 목록에 없는 값이 들어오는 경우다 — `apps/admin/src/pages/portfolio/case-studies/validation.ts:9,13-17` |
| 주소로 들어온 값 | 사용자가 고칠 수 있는 값을 믿지 않는다 | 목록의 `?industry=` 를 **허용값과 대조해** 모르는 값이면 `전체` 로 되돌린다 — `CaseStudyListPage.tsx:76-80`. 포트폴리오는 반대로 대조하지 않는다(분류가 운영자 정의라 닫힌 목록이 없다) |

## 4. 안정성

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 전 항목 | [포트폴리오](items.md) 준수 | 같다 |
| `null` 과 `[]` 의 혼동 | — | **이 자원에는 그 결함이 없다** — 선택지를 조회하지 않아 '못 불러온 것'과 '하나도 없는 것'이 섞일 자리가 없다. 포트폴리오의 가장 큰 안정성 구멍이 여기서는 성립하지 않는다 |
| 로그 기록 | — | **확인하지 못했다** |

## 5. 호환성

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 전 항목 | [포트폴리오](items.md) 준수 | 같다 |
| 해상도 | — | 목록이 5열로 좁다. **잘리는 칸이 다르다** — 포트폴리오는 고객사를, 이 화면은 성과를 한 줄로 자른다 |
| 지원 브라우저 | 공통 준수 | **확인하지 못했다** |

## 6. 운영성

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 전 항목 | [포트폴리오](items.md) 준수 | 같다 |
| 설정 관리 | 이 자원의 동작을 바꾸는 설정 | **없다** — 업종을 바꾸려면 코드를 고쳐야 한다. 관리 화면이 없다 |
| 배포 영향 | — | 공유 부품(노출 토글 열 · 미디어 하단부)을 고치면 포트폴리오가 함께 바뀐다 |

## 7. 유지보수성

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 전 항목 | [포트폴리오](items.md) 준수 | 같다 |
| 어휘의 정본 | — | 업종 목록·라벨·배지 색·정렬·필터가 **한 파일**에 모여 있다 — `case-studies/types.ts`. 통계 묶음에서 회원 등급 어휘가 두 벌로 갈린 것과 반대 사례다 |
| 업종을 늘릴 때 | — | 목록 상수 한 곳을 고치면 셀렉트·배지·필터가 함께 늘어난다. **다만 코드 배포가 필요하다** — 운영자가 늘릴 수 없다 |
| 이 화면이 갖는 조립물 | — | **없다** — 공유 부품과 공용 껍데기만 쓴다 |

## 8. 제약사항

| 항목 | 내용 |
|---|---|
| 전 항목 | [포트폴리오](items.md) 준수 — 백엔드 없음 · 페이지 상한 없음 · 업로드 없음 · 파일 규격 검사 없음 · 상세 화면 없음 · 검색 없음 · 내보내기 없음 · 이름 중복 미차단 |
| API 제약 | 경계는 `apps/admin/src/pages/portfolio/case-studies/data-source.ts:66` 의 `TODO(backend)` 한 줄이다 |
| 업종 | **코드 안의 6종 고정** — `제조` · `유통` · `금융` · `공공` · `의료` · `IT·서비스`. 운영자가 추가·삭제할 수 없다 |
| 본문 이미지 장수 | 최대 10장 — `case-studies/types.ts:40` |
| 본문 길이 | 과제·해결·성과 각각 500자 — `case-studies/types.ts:43` |
| 선행 데이터 | **필요 없다** — 포트폴리오와 달리 미리 만들어 둘 분류가 없다 |
