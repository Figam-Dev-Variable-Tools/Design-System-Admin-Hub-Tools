# 개인정보 처리방침 — 비기능 명세서

| 항목 | 내용 |
|---|---|
| 자원명 | 개인정보 처리방침 |
| 화면 ID | `SCR-CONTENT-PRIVACY` · `SCR-CONTENT-PRIVACY-DETAIL` · `SCR-CONTENT-PRIVACY-FORM` |
| 메뉴 경로 | 콘텐츠 관리 > 개인정보 처리방침 |
| 작성자 | Admin Hub 문서화 |
| 작성일 | 2026-07-22 |
| 버전 | 1.0 |
| 관련 기능 명세서 | [목록](../../FSD/content/privacy/index.md) · [상세](../../FSD/content/privacy/detail.md) · [등록·수정](../../FSD/content/privacy/form.md) |

**이 자원의 성격** — [약관 관리](terms.md)에서 **종류 축 하나만 뺀 것**이다(단일 문서라 고를 종류가 없다). 버전 이력 표·시행 판정·한계 셋(쓰기 미반영 · 권한 게이팅 없음 · 이탈 가드 없음)이 모두 같다. 아래 각 절은 그 문서 대비 **다른 것만** 적는다.

## 1. 성능

| 항목 | 요구 | 이 자원의 값 | 측정 방법 |
|---|---|---|---|
| 응답 시간 | 공통 준수 | 측정하지 않음 — 백엔드가 없다 | 백엔드 연동 후 재작성 |
| 조회 성능 | 공통 준수 | 목록 화면 **1건**(약관은 2건 — 종류 조회가 없다) · 상세 1건 · 폼(수정) 1건 | 코드 확인 |
| 대용량 데이터 처리 | 공통 준수 | **페이지네이션이 없다** — 버전 이력은 몇 건뿐이라 전부 그린다 | 코드 확인 |
| 검색 비용 | — | 검색은 화면에서 걸고 버전 표기만 본다 — `apps/admin/src/pages/content/privacy/PrivacyPage.tsx:100-107` | 코드 확인 |
| 본문 크기 | — | 전문은 최대 20,000자 — `apps/admin/src/pages/content/privacy/types.ts:42` | 코드 상수 확인 |

## 2. 사용성

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| UI 일관성 | 공통 준수 | 버전 이력 표는 약관과 **같은 공통 컴포넌트**다 — `apps/admin/src/shared/ui/VersionHistoryTable.tsx:3-4` |
| 조회 상태 보존 | 공통 준수 | **검색어 하나뿐**이다 — 필터도 페이지도 없어 이 화면이 직렬화할 조회 상태가 그것뿐이다 — `apps/admin/src/pages/content/privacy/PrivacyPage.tsx:9-11` |
| ⚠ 조회 실패가 화면 전체를 덮는다 | 실패해도 걸어 갈 길을 남긴다 | 약관 목록은 툴바를 남기고 표만 배너로 바꾸지만, **이 화면은 툴바까지 사라지고 배너만 남는다** — `apps/admin/src/pages/content/privacy/PrivacyPage.tsx:183-201`. 실패 상태에서는 '새 버전 등록'도 누를 수 없다 |
| 등록 버튼의 잠금 | — | 약관은 종류를 고르지 않으면 등록이 잠기지만, **이 화면은 처음부터 눌린다**(고를 종류가 없다) — `apps/admin/src/pages/content/privacy/PrivacyPage.tsx:212` |
| ⚠ 빈 상태의 사유 | — | **구분하지 않는다** — 약관과 같다 |
| ⚠ 미저장 보호 | — | **없다** — 약관 폼과 같은 결함이다. 반쯤 쓴 처리방침 전문이 링크 한 번에 사라진다 |
| 사용자 피드백 | 공통 준수 | 목록 2건 · 상세 1건 · 폼 2건. 실패는 전부 배너다 |

## 3. 보안

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 권한 | 리소스 키 | 잎 `page:/content/privacy` — **약관과 별개 키**다. 한쪽만 열어 줄 수 있다 |
| ⚠ 권한 게이팅 | 공통 준수 | **없다** — 약관과 같다. 개인정보 처리방침을 다루는 자원이라 이 결함의 무게가 크다 |
| 플랜(엔타이틀먼트) | 공통 준수 | 모듈 `cms.pages` — `apps/admin/src/shared/entitlements/module-resources.ts:71` |
| 개인정보 마스킹 | 어떤 필드를 어떤 규칙으로 가리는가 | **해당 없음** — 이 자원이 담는 것은 버전 표기·시행일·상태·전문이며, 전문은 **개인정보 처리 방식을 서술한 대외 공개 문서**다. 실제 개인정보를 담지 않는다 |
| 입력값 검증 | 화면 검증과 서버 검증을 구분한다 | 화면 검증만 있다 — `apps/admin/src/pages/content/privacy/validation.ts`. 약관 스키마와 규칙이 같고 버전 예시 문구만 다르다 |
| ⚠ 검증되지 않는 업무 규칙 셋 | — | 약관과 같다 — 시행일·상태의 앞뒤 · `시행중` 중복 · 버전 표기 중복 셋 다 막지 않는다 |
| 시행 중인 버전의 삭제 | — | ⚠ **막지 않는다** |
| 중복 제출 · 멱등키 | — | **없다** |

## 4. 안정성

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| ⚠ 쓰기가 반영되지 않는다 | — | **등록·수정·삭제가 픽스처를 바꾸지 않는다** — `apps/admin/src/pages/content/privacy/data-source.ts:78-104` |
| ⚠ 시행 전이를 수행하는 것이 없다 | — | 약관과 같다 — 상태는 사람이 고른 저장값이고 시행일이 지나도 바뀌지 않는다 — `apps/admin/src/pages/content/privacy/types.ts:37-39` |
| 장애 대응 | 조회 실패 시 무엇을 남기는가 | 목록은 **화면 전체**가 배너로 바뀐다(§2). 상세와 폼은 없는 id 와 오류를 같은 문장으로 말한다 |
| ⚠ 덮어쓰기 방지 | — | **충돌 처리가 없다** |
| 취소는 실패가 아니다 | 공통 준수 | 중단된 요청은 아무 표시도 만들지 않는다 — `apps/admin/src/pages/content/privacy/PrivacyPage.tsx:133` |
| 부분 실패 | — | 일괄 삭제는 전부 성공했을 때만 닫힌다 — `apps/admin/src/pages/content/privacy/PrivacyPage.tsx:167-178` |
| 로그 기록 | — | **확인하지 못했다.** ⚠ 처리방침 개정은 감사 대상이 될 가능성이 높은 조작이다 |

## 5. 호환성

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 지원 브라우저 | 공통 준수 | 리포에 정의되어 있지 않다 |
| 모바일 지원 | 공통 준수 | 측정하지 않음 |
| 해상도 | — | 버전 표는 열이 여섯으로 좁고, **왼쪽 필터 패널이 없어 약관보다 본문이 넓다** |
| 긴 전문의 표시 | — | 상세의 전문은 줄바꿈을 그대로 살리고 긴 낱말을 끊어 넘긴다 |

## 6. 운영성

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 로그 관리 | 공통 준수 | 확인하지 못했다 |
| 모니터링 | 공통 준수 | 없다 |
| 배포 영향 | 이 자원을 고치면 어느 화면이 함께 영향을 받는가 | 버전 이력 표를 고치면 [약관 관리](terms.md)도 함께 바뀐다. ⚠ **반대 방향의 연결은 없다** — 동의 이력의 재동의 산출은 **약관 종류만** 다루며, 이 자원을 읽는 배선을 코드에서 확인하지 못했다 — `apps/admin/src/shared/domain/terms-version.ts:31-39` |
| 설정 관리 | — | 이 자원의 동작을 바꾸는 설정값이 없다(종류 축이 없다) |
| 실패 재현 손잡이 | — | `?fail=list` · `?fail=detail` · `?fail=save` · `?fail=delete` — `apps/admin/src/pages/content/privacy/data-source.ts:48,57,78,88,100` |

## 7. 유지보수성

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 코드 확장성 | — | 공용 목록·폼 껍데기를 쓰지 않는다 |
| 화면 확장성 | 상태값이 늘 때 견디는가 | 상태가 늘면 라벨·색 지도에 타입 오류로 먼저 걸린다 — `apps/admin/src/pages/content/privacy/types.ts:9-20` |
| 페이지를 가로질러 import 하지 않는다 | — | 약관 페이지를 직접 import 하지 않는다 — 공유는 도메인을 모르는 공통 모듈로만 한다 — `apps/admin/src/pages/content/privacy/types.ts:3-4` |
| ⚠ 같은 코드가 두 벌이다 | 같은 판정을 두 곳에 적지 않는다 | 폼 본문(`apps/admin/src/pages/content/privacy/components/VersionForm.tsx`)은 약관의 같은 파일과 **구조가 거의 같다** — 종류 인자 하나와 문구만 다르다. 두 벌이면 언젠가 한쪽만 고쳐진다 |
| 규칙의 정본 하나 | — | 검증은 스키마 하나, 시행본 판정은 `isCurrent` 하나가 갖는다 |

## 8. 제약사항

| 항목 | 내용 |
|---|---|
| 외부 시스템 연계 | 홈페이지는 이 리포 밖이다 |
| API 제약 | **백엔드가 없다** — `apps/admin/src/pages/content/privacy/data-source.ts:48`(목록) · `:57`(단건) · `:78`·`:88`·`:100`(CRUD) |
| 페이지 크기 상한 | **없다** — 페이지네이션 자체가 없다 |
| 입력 길이 제한 | 버전 표기 20자 · 전문 20,000자 — `apps/admin/src/pages/content/privacy/types.ts:41-42` |
| 시행 판정 | **저장된 상태값이 전부다.** 시행일은 표시되기만 한다 — `apps/admin/src/pages/content/privacy/types.ts:37-39` |
| 동의 이력과의 연결 | **없다** — 재동의 산출은 약관 종류만 읽는다(§6) |
| 파일 용량 제한 | 해당 없음 — 이 자원에 파일 입력이 없다 |
| 업로드 가능 확장자 | 해당 없음 — 같은 이유 |
