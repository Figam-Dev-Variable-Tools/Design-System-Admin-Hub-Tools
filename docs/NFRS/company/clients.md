# 고객사 — 비기능 명세서

| 항목 | 내용 |
|---|---|
| 자원명 | 고객사 |
| 화면 ID | `SCR-COMPANY-CLIENTS` · `POP-COMMON-LOGO-FORM`(공유 팝업) |
| 메뉴 경로 | 기업 관리 > 고객사 |
| 작성자 | Admin Hub 문서화 |
| 작성일 | 2026-07-22 |
| 버전 | 1.0 |
| 관련 기능 명세서 | [목록](../../FSD/company/clients/index.md) · [로고 등록·수정 팝업](../../FSD/_common/pop-logo-form.md) |

**이 자원의 성격** — **[파트너사](partners.md)와 한 화면 모듈을 공유한다.** 주입되는 값 셋(자원 이름 `clients` · 엔티티 라벨 `고객사` · 데이터 소스)만 다르다 — `apps/admin/src/pages/company/clients/ClientsPage.tsx:8`. 그래서 이 문서는 **차이만** 적는다. 각 절의 나머지 내용은 [파트너사](partners.md)의 같은 절을 그대로 따른다.

## 1. 성능

파트너사와 **같다** — 조회 1건 · 페이지네이션 없음 · 재조회 중 표 유지 · 재정렬 요청 중복 방지 · 목록에 썸네일 없음.

| 항목 | 요구 | 이 자원의 값 | 측정 방법 |
|---|---|---|---|
| 응답 시간 | 공통 준수 | 측정하지 않음 — 백엔드가 없다 | 백엔드 연동 후 재작성 |
| 조회 성능 | 공통 준수 | 목록 1건. **파트너사와 조회 대상이 갈린다** — 자원 이름이 다르므로 캐시 키도 다르다 | 코드 확인 |
| 대용량 데이터 처리 | 가상화 없이 대량 행을 그리지 않는다 | ⚠ 페이지네이션도 가상화도 없다 — 파트너사와 같은 이유(순서가 곧 의미다) — `apps/admin/src/pages/company/logo-list/LogoListPage.tsx:6-8` | 코드 확인 |
| 쌓이는 속도 | — | ⚠ **고객사는 파트너사보다 빨리 쌓일 수 있다**(거래처가 늘 때마다 늘어난다). 전량 렌더의 위험은 이쪽이 먼저 온다 — 다만 실측값이 없어 **측정하지 않음** | — |

## 2. 사용성

파트너사와 **같다** — 검색어를 주소가 소유하고, 한글 조합 중에는 조회하지 않으며, 목록에서 노출을 켜고 끄고, 드래그와 이동 버튼으로 순서를 바꾼다. 같은 한계도 그대로다: **재정렬이 막힌 이유를 말하지 않는다** · **빈 상태의 사유를 구분하지 않는다** · **라이브 영역이 없다**.

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 두 화면의 검색어가 섞이지 않는다 | — | 두 라우트가 **각자 자기 주소**를 가지므로 파트너사에서 건 검색이 이 화면에 따라오지 않는다 — `apps/admin/src/pages/company/logo-list/LogoListPage.tsx:9-10` |
| 문구의 엔티티 값 | — | 검색 라벨·추가 버튼·전체 선택·빈 상태·표 설명·조회 실패 배너의 `{엔티티}` 자리에 `고객사` 가 들어간다 — `apps/admin/src/pages/company/logo-list/LogoListPage.tsx:268,273,318` · `apps/admin/src/pages/company/logo-list/LogoListTable.tsx:119,127,151` |

## 3. 보안

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 권한 | 리소스 키 | 잎 `page:/company/clients`. **파트너사와 별개 키**이므로 한쪽만 열어 줄 수 있다 — `apps/admin/src/shared/permissions/resources.ts:66-68` |
| ⚠ 권한 게이팅 | 누를 수 없는 것을 보여 주지 않는다 | **없다** — 파트너사와 같은 결함이다(한 모듈을 공유하므로 결함도 공유한다) |
| 플랜(엔타이틀먼트) | 공통 준수 | **걸리지 않는다** — 모듈 대응표에 없다 |
| 개인정보 마스킹 | 어떤 필드를 어떤 규칙으로 가리는가 | **가리는 필드가 없다** — 이름(법인명)·로고·링크·순서·노출 여부뿐이다. ⚠ **다만 고객사 명단 자체가 대외 공개 여부를 계약으로 정하는 값일 수 있다** — 그 통제 수단은 목록의 노출 토글 하나이며, 어드민 안에서는 노출을 꺼도 이름이 보인다 |
| 입력값 검증 | — | 팝업이 갖는다(파트너사와 같은 스키마) — `apps/admin/src/pages/company/logo-list/validation.ts` |
| ⚠ 중복 제출 | — | **없다** — 파트너사와 같다 |

## 4. 안정성

파트너사와 **같다** — 쓰기가 반영되고, 수정·삭제·노출 토글은 409 로 막히며, **재정렬만 그 방어가 없고**, 충돌 사유를 갈라 보여 주지 않으며, 이미지가 서버로 가지 않는다.

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 자원이 갈린다 | 한쪽의 실패가 다른 쪽을 건드리지 않는다 | 어댑터가 **자원 이름을 스코프로** 갖는다 — 고객사의 실패 재현이 파트너사에 걸리지 않고, 새 항목 id 도 자원 이름을 접두로 갖는다 — `apps/admin/src/pages/company/logo-list/adapter.ts:49,56` |
| 로그 기록 | — | **확인하지 못했다** |

## 5. 호환성

파트너사와 **같다** — 지원 브라우저는 리포에 정의되어 있지 않고, 모바일은 측정하지 않음이며, ⚠ **가로 스크롤 감싸개가 없다**. 드래그의 대체 경로(이동 버튼)와 파일 선택 방식도 같다.

## 6. 운영성

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 로그 관리 | 공통 준수 | 확인하지 못했다 |
| 모니터링 | 공통 준수 | 없다 |
| 배포 영향 | 이 자원을 고치면 어느 화면이 함께 영향을 받는가 | ⚠ **이 화면을 고치면 [파트너사](partners.md)가 함께 바뀐다.** 한쪽만 다르게 하려면 설정 인자를 늘려야 한다 |
| 설정 관리 | — | 주입되는 값 셋뿐이다 — `apps/admin/src/pages/company/clients/ClientsPage.tsx:8` |
| 실패 재현 손잡이 | — | `?fail=clients:save` 처럼 **이 자원만** 실패시킬 수 있다 — `apps/admin/src/pages/company/logo-list/adapter.ts:49,54,62,68,74,79` |
| 데이터가 섞이지 않는다 | — | 두 자원은 각자 시드로 각자 어댑터 인스턴스를 만든다 — `apps/admin/src/pages/company/logo-list/adapter.ts:28-30` |

## 7. 유지보수성

파트너사와 **같다** — 한 모듈을 config 로 공유하고, 재정렬 UI 는 디자인 시스템 것을 쓰며, 순수 규칙은 한 파일에 모여 있다.

| 항목 | 요구 | 이 자원의 값 |
|---|---|---|
| 두 화면이 한 모듈이다 | 같은 것을 두 벌로 두지 않는다 | 화면 파일은 **여덟 줄짜리 얇은 배선**이다 — 설정만 주입하고 나머지는 공유 모듈이 한다 — `apps/admin/src/pages/company/clients/ClientsPage.tsx:1-9` |
| 결함도 공유한다 | — | ⚠ 그 대가로 §3 의 권한 게이팅 부재, §5 의 가로 스크롤 부재 같은 결함이 **두 자원에 동시에 존재하고 동시에 고쳐진다** |

## 8. 제약사항

파트너사와 **같다**. 다른 것만 적는다.

| 항목 | 내용 |
|---|---|
| API 제약 | **백엔드가 없다** — `apps/admin/src/pages/company/clients/data-source.ts:35`(CRUD · 순서 · 노출). 파트너사와 **다른 엔드포인트**로 갈린다 |
| 파일 업로드 | **업로드 엔드포인트가 없다** — 로고는 브라우저 안 참조로만 남는다 |
| 파일 용량 제한 | 이미지 최대 5MB — `packages/ui/src/molecules/ImageUploadField/ImageUploadField.tsx:136` |
| 업로드 가능 확장자 | 안내 표기는 `PNG · JPG · GIF · 최대 5MB` — `packages/ui/src/molecules/ImageUploadField/ImageUploadField.tsx:271` |
| 페이지 크기 상한 | **없다** |
| 입력 길이 제한 | 이름 60자 — `apps/admin/src/pages/company/logo-list/types.ts:61` |
| 이름 중복 | **막지 않는다** |
| 순서 변경의 조건 | **검색어가 비어 있을 때만** |
| 권한 게이팅 | **없다**(§3) |
